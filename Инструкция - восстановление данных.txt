программа

программа = СкачатьПрограммуНаНеповреждённыйДиск()
программа.Открыть()
Если ИнформацияОтобразилась(), то
	Если (ИзвестнаОбластьПамяти())
		НастроитьГраницыСканирования()
	Если(Сканировать() == неУдалосьНайтиЧастьДанных), то
		программа.восстановлениеПоСигнатурам()
	Если(программа.Сохранить(C:/Programm Files (x86)) == ОшибкаЧтения), то
		ОбратитьсяКСпециалисту()
	Иначе
		УбедитьсяВсёЛиСохранилось(программа)
	
иначе делать Если(ПрограммаОтоброжаетсяНоНетНужногоРаздела(программа) == true)
	Если (программа.УглубленноПросканировать() == успушно), то
		goto(3)
	иначе делать Если(программа.СканированиеПоВсемуУстройству() == успешно)
		goto(3)
	иначе делать
		программа = НайтиДругуюПрограмму()
		goto(3)
иначе делать Если(УстройствоОтсутствуетВСпискеНоОпределяетсяСредствамиОС())
	Если(накопительБылПодключёнПослеЗапускаПрограмма)
		программа = НайтиДругуюПрограмму()
		goto(3)
иначе делать Если(УстройствоНеОпределяетсяСредствамиОперационнойСистемы())
	Если(ПроверитьПравильностьПодключения() == неВиден)
		ОбратитьсяКСпециалистамИлиДругиеМатериалы()

	
